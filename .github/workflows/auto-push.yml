name: Auto-Push on Major Updates

# Trigger on push to main branch (for tracking)
on:
  push:
    branches:
      - main
    paths:
      # Backend changes
      - 'Project_AccountingLegalChatbot/backend/**'
      - 'Project_AccountingLegalChatbot/backend/main.py'
      - 'Project_AccountingLegalChatbot/backend/requirements.txt'
      
      # Frontend changes
      - 'Project_AccountingLegalChatbot/frontend/src/**'
      - 'Project_AccountingLegalChatbot/frontend/package.json'
      
      # Configuration changes
      - 'Project_AccountingLegalChatbot/.env.example'
      - 'Project_AccountingLegalChatbot/config.py'
      
      # Documentation
      - 'README.md'
      - '.gitignore'

jobs:
  detect-major-update:
    runs-on: ubuntu-latest
    
    outputs:
      is_major: ${{ steps.detect.outputs.is_major }}
      commit_msg: ${{ steps.detect.outputs.commit_msg }}
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
      
      - name: Detect major changes
        id: detect
        run: |
          # Get last commit subject line only (multi-line bodies break $GITHUB_OUTPUT)
          COMMIT_MSG=$(git log -1 --pretty=%s)
          # Escape for GITHUB_OUTPUT (newlines and % are unsafe)
          COMMIT_MSG_SAFE="${COMMIT_MSG//'%'/'%25'}"
          echo "commit_msg=$COMMIT_MSG_SAFE" >> $GITHUB_OUTPUT
          
          # Check for major update keywords
          if echo "$COMMIT_MSG" | grep -iE "(feature|fix|major|update|improvement|refactor|security|hotfix|critical)"; then
            echo "is_major=true" >> $GITHUB_OUTPUT
            echo "🔴 MAJOR UPDATE DETECTED"
          else
            echo "is_major=false" >> $GITHUB_OUTPUT
            echo "⚪ Minor update"
          fi
      
      - name: Log detection result
        run: |
          echo "Major Update: ${{ steps.detect.outputs.is_major }}"
          echo "Message: ${{ steps.detect.outputs.commit_msg }}"

  create-release:
    needs: detect-major-update
    if: needs.detect-major-update.outputs.is_major == 'true'
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Get version
        id: version
        run: |
          VERSION=$(git describe --tags --always)
          echo "version=$VERSION" >> $GITHUB_OUTPUT
      
      - name: Notify push
        run: |
          echo "✅ Major update auto-pushed!"
          echo "Commit: $(git log -1 --oneline)"
          echo "Branch: $(git rev-parse --abbrev-ref HEAD)"
          echo "Time: $(date -u +'%Y-%m-%d %H:%M:%S UTC')"

  verify-repository:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Verify repository integrity
        run: |
          echo "📊 Repository Status Check"
          echo "=========================="
          echo "Total commits: $(git rev-list --count HEAD)"
          echo "Total files: $(git ls-tree -r HEAD --name-only | wc -l)"
          echo "Repository size: $(du -sh . | cut -f1)"
          echo ""
          echo "✓ Repository is healthy"
